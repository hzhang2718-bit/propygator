========================================================================
RECONCILE: shared Langmuir K (the box inherits the Tier-A accommodation)
========================================================================
  K_exp (cd_core)   = 1.9230e+06 1/Pa
  K_gen (generator) = 1.9227e+06 1/Pa
  K rel diff = 0.017%   [PASS < 1%]
  (Identical to the sphere reconcile(i); both anchor alpha=0.90. The two
  per-face pipelines below run at the shared K_exp, so K does not confound
  the physics comparison.)
  Note: species masses match except He (cd_core 4.0026 vs generator
  4.002602 amu) -- a 7th-digit difference on a trace species, sub-ppm on CdA.

========================================================================
SHARED PER-FACE PHYSICS: closed form off IDENTICAL inputs (expect ~eps)
========================================================================
  Cd_face(theta) per species, both evaluators fed the same (theta, V, T,
  mass, alpha). This is the term-by-term proof the two reconstructions are
  the same Schaaf-Chambre/Sentman physics -- pressure AND shear -- across
  the full [0, pi] axis (the shear term only shows up at large theta).
  per-species per-face closed form: max abs diff = 8.88e-16  max rel diff (Cd>0.1) = 3.82e-16
  mass-flux-weighted per-face Cd (identical V, alpha): max abs diff = 3.87e-07  (He-mass nuance only)

========================================================================
END-TO-END: assembled CdA = sum_i Cd_i * A_i, both pipelines, one MSIS set
========================================================================
  Experiment uses v_rel (spherical radius); generator uses the geocentric
  radius -- each its own convention, shared K_exp. Swept over a Fibonacci
  fan of flow directions x radius x density for two convex geometries.
  3570 (condition x altitude x attitude) points, 2 geometries, lat -60..70
  geometry                  CdA range  phys+speed max      mean  pure-phys max
  box 2.0x1.5x1.0             >=3.725        0.0364%   0.0063%      1.23e-07
  plate 1.0x1.0x0.02          >=0.139        0.1193%   0.0122%      2.48e-07
  GLOBAL max ABSOLUTE per-face diff = 3.6515e-04  (smallest force-relevant Cd ~0.07; eps_abs gate = 0.02)

========================================================================
VERDICT [PASS]: the two per-face reconstructions share the same closed
  form to 8.9e-16 (machine eps); end-to-end they agree on the
  force-relevant CdA to 0.1193% (phys+speed, shared K), with the
  K calibration agreeing to 0.0170% -- full independent agreement is
  bounded by the product, << 1%. Per-face absolute agreement 3.7e-04.
========================================================================
All Tier B per-face cross-validation checks PASS (the sec. 5 equivalence invariant holds for BoxFaceCd).
