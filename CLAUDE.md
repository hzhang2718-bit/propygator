# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project state

**propygator** is a Python library for orbital simulation and satellite tracking, built on [Orekit](https://www.orekit.org/) via the `orekit_jpype` wrapper. The package is installable, JVM-free on import, and 882 tests pass.

**Shipped on `main`** (each release was its own branch — or, for `v0.5.0`, a batch of five — squash-merged and tagged; designs + build plans archived under `docs/history/`):
- `v0.1.0` — Feature 1.1 numerical propagator + its drag-validity & altitude-guards addendum
- `v0.2.0` — ECEF-nadir attitude + direction-marker plot glyphs addendum
- `v0.3.0` — Feature 1.3 TLE propagator
- `v0.4.0` — Feature 1.4 real-time tracker ("TLE Live Dashboard")
- `v0.5.0` (2026-07-07) — the five-part "general upgrades" batch: Tier B `BoxFaceCd` drag, the live-dashboard mutate-in-place layer, ECEF `InPlaneTracking`, the lumped planetary third body, and civil time zones + progress reporting — each its own branch squash-merged untagged, one tag after the last (details in the five paragraphs below)

**Tier B drag (`BoxFaceCd`)** — the first of the five independent "general upgrades" that shipped together as `v0.5.0`. `BoxFaceCd` is a per-face, incidence-resolved free-molecular drag table for a convex box — the shipped successor to the deferred `IncidenceVariableCd` skeleton (now gone; box-drag misuse fails at *construction*). Shipped: the Orekit-free generator `scripts/generate_box_face_cd_table.py` → `data/box_face_cd_default.npz`, cross-validated against the runtime kernel (≪ 1% on `CdA`); the pure-Python `BoxFaceCd` type in `spacecraft.py` (re-exported beside `VariableCd`, convex-box-only, `solar_array_area_m2 == 0`); and the per-face drag path inside the shared `DragSensitive` proxy. Benefit study → **GO, ship with caveat**: the per-face table is material only for large, flat satellites flown edge-on (e.g. a solar/drag sail), where a recalibrated constant Cd can't absorb the tangential-shear divergence (~2× along-track); for conventional buses the effect is negligible.

**ECEF InPlaneTracking has landed** (branch `feature/ecef-attitudes`, the third general upgrade feeding `v0.5.0`; contract: `general-upgrades-1.md` §"ECEF InPlaneTracking" with its **Outcome** note — Supercessions folded back into `features.md` §1.1 / `architecture.md` §13 / the `BoxFaceCd` docstring; build plan archived at `docs/history/build-plan-ecef-attitudes.md`). `InPlaneTracking` gained `velocity_reference: "inertial" | "ecef"` — `ecef` re-aims the mode's **primary** alignment onto the Earth-relative wind `v_rel = v − ω⊕×r` (the *inverse* of NadirPointing's secondary-slot swap), reusing the shipped ECEF `TargetProvider` verbatim, so a feathered sail's big faces hold exactly zero angle of attack to the co-rotating flow; +Z becomes best-effort on the orbit normal, off by the out-of-plane wind angle (≤ ~3.8° in LEO, zero at i=0). Default is bit-identical; the metadata token is now **always** `in_plane_tracking:vel=<ref>`. Shipped-mode benefit study (`experiments/ecef-attitude-benefit/`, conda env — no experiment venv): ~1.12× drag-effect cut / −273 km along-track over 5 d for a 1 m² / 0.5 kg sail at 500 km SSO solar max — modest and inclination-dependent; shipped as an attitude-fidelity fix that makes the `BoxFaceCd` feathered regime declaratively reachable. **Verified at build:** the documented `v_rel ≈ 0` failure raises only at *exactly* zero (unreachable in floating point) — a near-GEO run completes carrying a physically meaningless attitude (whole-frame noise, since the corrupted target is the primary); LEO is the validated domain. `LofOffset`-ecef stays the named follow-on; `LofAligned`-ecef is permanently redundant (InPlaneTracking-ecef up to an axis permutation).

**Live Dashboard Blitting has landed as the mutate-in-place layer only** (branch `feature/live-dashboard-blitting`, the second general upgrade feeding `v0.5.0`). Contract: `general-upgrades-1.md` §"Live Dashboard Blitting" (see its **Outcome** note; Supercessions folded back into `features.md` §1.4); build plan archived at `docs/history/build-plan-live-dashboard-blitting.md`. `tracking/live.py`'s display layer went from clear-and-redraw to **build-once / mutate-in-place at `blit=False`**: the scene is built once inline in `live_track` (no `init_func`, no `_init_scene` helper — the code review removed the `init_func`, since FuncAnimation's initial draw runs `update()` itself and fully populates the first frame), a mutate-in-place `_draw_static` refreshes the static curves on rebuild (never `ax.clear()`), the dynamic now-indicators (rotated-heading marker, cursors, sky glyphs, suptitle readout) are persistent artists mutated per tick, and every axis limit is pinned — so toolbar zoom/pan survives every redraw and buffer rebuild (the headline win). The altitude/speed y-limits are **widened (never shrunk) on a rebuild whose curve would exceed them** (`_widen_ylim_if_untouched` — e.g. a buffer shorter than one orbit), and only while the user hasn't zoomed/panned that panel. Chunk 1's additive seams: `_draw_altitude`/`_draw_speed`/`_draw_sky_track` return the `Line2D`s they create (`_draw_ground_track` still returns the mappable). **The blit layer proper (Chunks 3–4) was declined at Checkpoint A** — at 1 Hz the flicker-free/CPU win didn't justify the private-`_blit_cache` matplotlib coupling; blitting stays a named later optimization. `live_track`'s §1.4 signature and the `_TrackerEngine` are unchanged. Manual GUI smoke check: `run/live_dashboard_demo.py` (gitignored; `--fast` forces a rebuild in ~2 min).

**Lumped planetary third body has landed; Earth radiation pressure is blocked on upstream Orekit** (branch `feature/additional-perturbations`, the fourth general upgrade feeding `v0.5.0`; contract: `general-upgrades-1.md` §"Planetary Third-Body & Earth Radiation Pressure" with its **Outcome** note — planets Supercessions folded back into `features.md` §1.1 / `architecture.md` §13; build plan archived at `docs/history/build-plan-additional-perturbations.md`). `ForceModelConfig` gained `planets_third_body: bool = False` — **one lumped toggle** (deliberately not per-planet booleans) wiring `ThirdBodyAttraction` for the pinned seven-planet set (Mercury–Neptune, heliocentric order; accessors in `core/bodies.py`, off the DE ephemeris already in orekit-data — no new data), one `third_body:planets` metadata token after `third_body:moon`, off in every preset (defaults bit-identical). Honestly scoped as completeness: ~1e-10–1e-13 of central gravity (GEO pin ~0.15 m over 3 d at the fixed test epoch; LEO negligible). **The companion `earth_radiation` (Knocke albedo + IR) was fully built and then reverted, unshipped:** its effect test exposed a horizon-bound defect in `KnockeRediffusedForceModel` present in every installable Orekit (≤ 13.1.5 — visible-cap bound `asin(R/r)` instead of `acos(R/r)`; ~2.4–3× hot at LEO even in eclipse, ~10–20× cold at GEO; fixed upstream in Orekit 13.1.6, 2026-06-03, but no orekit_jpype wrapper ≥ 13.1.6 exists yet). The parked runtime patch + self-verdicting bug probe + resume recipe live in `experiments/earth-radiation/` — when a fixed wrapper appears: clone-and-test env (never mutate the working one), run the probe, apply the patch, re-run the resolution benchmark (the fix changes LEO convergence), ship as its own branch.

**Civil time zones & progress reporting have landed** (branch `feature/ux-improvements`, the fifth general upgrade feeding `v0.5.0`; contract: `general-upgrades-1.md` §"Civil Time Zones & Progress Reporting" with its **Outcome** note — Supercessions folded back into `features.md` §1.1/§1.4 + `architecture.md` §Logging + this file; build plan archived at `docs/history/build-plan-ux-improvements.md`). **Part A:** `live_track` gained trailing keyword-only `tz: USTimeZone | tzinfo | None = None` — the pinned seven-member `USTimeZone` enum + `_resolve_tz` in `core/time.py` (top-level export) lower to `zoneinfo`, re-expressing **only the suptitle readout clock** in DST-correct US civil time (`None` stays byte-identical UTC; raw `tzinfo` is the non-US escape hatch; civil zones are display-only, never a `TimeScale`); `tzdata` is now a declared dep (conda-forge name **`python-tzdata`**, PyPI name `tzdata`). **Part B:** `propagate_numerical` gained `progress: bool | ProgressCallback = True` — the **one deliberate edit to the frozen §1.1 signature** — default-on ASCII stderr status lines from `core/progress.py`'s `_ProgressReporter`, fed by an `OrekitFixedStepHandler` `@JImplements` proxy at `span/1000` cadence: start line **before** JVM boot, 10%/~5 s cadence on a TTY, coarse 25/50/75 off-TTY (pytest/CI/notebooks), honest `done` / `stopped at NN% | <reason>` / `failed at NN%` final line on **every** exit path (one `try/finally` from `reporter.start()` through classification); `progress=False` silent, a callable gets the 0→1 fraction and nothing prints (`ProgressCallback`, top-level export). Build-verified: the step normalizer doesn't guarantee an endpoint tick, so the proxy's `finish(final_state)` forwards the true final fraction. 1.5/1.2 inherit both surfaces from birth (`tz=` on 1.5; `progress` on both — 1.2 drives the reporter's indeterminate `step()` mode, already built + tested).

With `v0.5.0` shipped, the remaining features in architecture §12 order are **1.5 passes (`find_passes`) → 1.2 TLE fitter (`fit_tle`)** — the only two `README.md` target verbs not yet shipped (both `features.md` designs **NOT STARTED**; 1.5 reuses the shipped `look_angles` primitive, 1.2 is the hardest and treated as a plus).

### Where things live

All shipped code is on `main`; the full public surface is re-exported from the top-level `__init__.py`, which stays JVM-free.

- **`_orekit_init.py`** — lazy-JVM init: `init()`, `clear_cache()`, orekit-data resolution, `_ensure_started()`.
- **`core/`** — the pure-Python data model (`Epoch`/`TimeScale`, `Frame`, `State`, `Trajectory`, `KeplerianElements`, `Orientation`, `GroundStation`/`GeodeticPosition`/`Pass`, `AzElRange`, `TLE`, exceptions), all constructible + validating with no JVM (the "safe before init" surface). Plus the Orekit-crossing conversion layer (`to_orekit`/`to_frame`/`to_keplerian`/`Trajectory.at`/`to_geodetic`; `Epoch.to_orekit` is the reference lazy-bootstrap pattern), `bodies.py` (WGS84 Earth (ITRF) + Sun/Moon/seven-planet accessors behind `_ensure_started()`), `sampling.py` (shared output grid), `catalogs.py` (name→NORAD), the `TLE` type in `tle.py` (parse/checksum/`from_norad_id`/`from_state_unfitted`), the sky-view kernel in `observation.py` (`look_angles`/`sun_look_angles`/`moon_look_angles`/`look_angles_track`/`observer_snapshot`), the civil-display `USTimeZone` enum + `_resolve_tz` in `time.py` (v0.5.0; display-only, never a `TimeScale`), and `progress.py` (v0.5.0) — `_ProgressReporter` (determinate + indeterminate modes, TTY gate, 10%/heartbeat throttle, idempotent `close()`) + the public `ProgressCallback` alias behind every long-running verb's `progress=`.
- **`propagation/`** — `propagate_numerical` (`numerical.py`) + frozen config dataclasses (`ForceModelConfig`, `IntegratorConfig`, `SpacecraftConfig`/`SpacecraftGeometry`/`VariableCd`/`BoxFaceCd`) + the 7-mode attitude family (`LofAligned`/`LofOffset`/`Inertial`/`SunPointing`/`NadirPointing`/`InPlaneTracking`/`CustomAttitude`; `NadirPointing` **and** `InPlaneTracking` take `velocity_reference="inertial"|"ecef"` — one shared custom `TargetProvider`, `attitude._build_ecef_velocity_target_provider`, in the *secondary* slot for nadir and the *primary* slot for in-plane) + the altitude/regime **guard system** (`guards.py`). Forces: gravity + third-body (Sun/Moon + the lumped seven-planet `planets_third_body` toggle) + drag + SRP + solid/ocean tides + relativity; sphere & `box_and_panels` geometry. **All drag routes through one shared custom `DragSensitive` proxy** (sphere/box, fixed/table) so the warn-once hook is uniform. Guards: terminal radius event detectors for impact (`r=R⊕`) / escape (`r≈327,000 km`); `AltitudeLimits` (the `limits=` keyword, tightest-of nesting); the min-step re-entry catch with partial-trajectory recovery; the two-tier drag-regime warn-once (Kn floor + table edges — the Kn floor scanned at runtime from a conservative composition embedded in `guards.py`, since Orekit exposes only total density and pymsis is not a runtime dep); and stop-and-report assembling a partial `Trajectory` via the additive `terminated`/`termination_reason`/`termination_epoch` metadata keys. Detailed design: the drag-validity addendum doc. The v0.5.0 `progress=` param (default `True`) streams throttled status through an `OrekitFixedStepHandler` `@JImplements` proxy into the `core/progress.py` reporter — one wide `try/finally` (from `reporter.start()` through termination classification) guarantees an honest final line on every exit path, including setup failures.
- **`tle/`** — `propagate_tle` (`propagator.py`); `fetch_tle` + 6h/24h on-disk cache (`sources.py`, CelesTrak-only; transport failures wrapped as `TLEFetchError`). SGP4/SDP4 validated against Vallado AIAA 2006-6753 (cases 00005 + 08195).
- **`tracking/`** — realtime primitives `current_state` (TEME) / `current_ground_position` (`realtime.py`); the live dashboard `live_track` (`live.py`) — a headless-testable `_TrackerEngine` maintaining a now-centred rolling `Trajectory` buffer (drain-triggered re-propagation, auto-refetch for fetched targets) driving `FuncAnimation` over 3 panels (ground track / altitude / speed) or 4 (+ sky polar with a `GroundStation`). The display layer is build-once / mutate-in-place at `blit=False` (pinned limits, persistent artists — toolbar zoom/pan survives redraws and rebuilds; the v0.5.0 mutate-in-place upgrade). `live_track(tz=)` re-expresses the suptitle readout clock in a US civil zone (v0.5.0; resolved once at entry via `core.time._resolve_tz`, threaded to the tz-ready `_format_clock` seam). `live.py` is pure composition — lazy-imports plotting + matplotlib, no Orekit. `StaleTLEWarning(UserWarning)` is filtered per-session.
- **`io/exports.py`** — `export_csv` (16 default columns + additive `keplerian`/`sun`/`mean_anomaly` group tokens + metadata header); `export_all` (summary PNG + 3D HTML + CSV).
- **`plotting/`** — `plot_summary`/`plot_ground_track`/`plot_3d`/`plot_altitude`/`plot_speed`/`plot_sky_track` (Plotly for `plot_3d`, matplotlib for the rest), the shared `style.py` per-figure context manager, the bundled Natural Earth coastline `basemap.py`. The ECEF-nadir & direction-markers addendum added `NadirPointing(velocity_reference='ecef')` (a custom `TargetProvider`, `attitude._build_ecef_velocity_target_provider`) and the ground-track heading triangle / 3-D velocity cone glyphs (`plot_3d` sets explicit scene ranges + manual aspect ratio via `plotting/trajectories._scene_aspect_ratio` so the globe renders round at any flatness).

**Permanent deferrals (raise `NotImplementedError`):** UT1 everywhere (`core/time.py` + the `Trajectory` UT1-epoch guard) — needs EOP data. (The `Frame.to_orekit` raise is a defensive fallback for an unmapped enum member, not a deferral.)

## Source-of-truth documents

Read these before implementing anything — decisions in them are deliberate; don't silently diverge. **Status headers in build plans are the maintainer's — trust the git log for true status.** Build plans share a per-chunk Goal/Create-Edit/Reuse/Verify rhythm and are working blueprints, not contracts (the matching design doc wins on conflict).

**Active / binding:**
- `docs/architecture.md` — the single most important file: cross-cutting design (data model, module structure, conventions, key decisions, testing). Section numbers (§3, §6, §10, §11…) are cited throughout the code.
- `docs/features.md` — per-feature sub-designs. **§1.1 / §1.3 / §1.4 are the binding contracts** for the shipped numerical-propagator / TLE-propagator / live-tracker surfaces — every signature, preset, validation rule, metadata grammar, output (don't change an implemented signature). 1.2/1.5 are NOT STARTED. (§1.4's buffer *magnitudes* — `output_step`/`half_window_s`/`refresh_s` — are tunable placeholders, not contract.)
- `docs/general-upgrades-1.md` — **binding contract for the v0.5.0 general upgrades: Tier B drag (`BoxFaceCd`, shipped), Live Dashboard Blitting (shipped as the mutate-in-place layer only — blit declined at Checkpoint A), ECEF InPlaneTracking (shipped in full), Planetary Third-Body & Earth Radiation Pressure (planets shipped; ERP blocked on upstream Orekit), and Civil Time Zones & Progress Reporting (shipped in full) — see each section's Outcome note.** Each section's **Supercessions** list won on conflict until its wrap-up folded it back — all shipped sections are now folded back (Tier B into `architecture.md` §13 + `features.md` §1.1; blitting into `features.md` §1.4 in its partial form: mutate-in-place at `blit=False`, blitting itself still deferred; ECEF InPlaneTracking into `features.md` §1.1 + `architecture.md` §13 + the `BoxFaceCd` docstring; planets into `features.md` §1.1 + `architecture.md` §13; civil time + progress into `features.md` §1.1 (`progress` signature + "Progress reporting" section) / §1.4 (`tz=`) + `architecture.md` §Logging + `CLAUDE.md`). Everything else in those docs stands. The batch shipped as `v0.5.0` (2026-07-07); the doc **stays in `docs/`** because its ERP half remains the binding contract for the parked, upstream-blocked `earth_radiation` resume.
- `docs/project_meta.md` — governance/distribution (MIT, GitHub-only, tier-(a) personal portfolio, minimal CI). *Supersedes* the tier-(b) framing that leaks into a few spots of `architecture.md`.
- `docs/release-process.md` — the feature-branch → tagged-release walkthrough (release prep → squash-merge → annotated tag → push/cleanup → verify). Reinforces the version-bump-needs-reinstall gotcha and `git branch -D` (not `-d`) for squash-merged branches. The maintainer runs the git steps.
- `docs/changelog-guidelines.md` — the Keep-a-Changelog rhythm + grouping conventions (CHANGELOG is hand-maintained).
- `docs/experiments_venv.md` — the throwaway **venv** (not the conda env) that runs the `experiments/` reference scripts (needs `scipy` + `pymsis`; OpenBLAS wheels sidestep the Windows MKL crash). Setup, hygiene, verified install snapshot.

**Reference / non-binding** (each self-flags; `architecture.md`/`features.md` win on conflict):
- `docs/prospective-forces-and-progress-findings.md` — feasibility notes on three candidate `propagate_numerical` enhancements (planetary third-body gravity, Earth radiation pressure, a progress bar/log). All three now resolved (items 1 and 3 shipped, item 2 blocked upstream — see its status header); kept as the mechanism reference (§4 is the progress step-handler probe). Orekit-API claims introspected 2026-06-20.
- `docs/orekit_setup_reference.md` — the working Orekit/JPype environment + Java↔Python boundary patterns. **Self-flags as possibly out of date; verify before relying on it.**

**Archived under `docs/history/`** (built & shipped; kept as the historical record of *why* — full design docs + sequenced build plans):
- Feature 1.1 numerical propagator (`build-plan-feature-1.1.md`, 13 chunks; carries the full git feature-branch → PR → merge walkthrough) plus its two addenda — drag-validity & altitude-guards (`feature-1.1-addendum-drag-validity-and-altitude-guards.md` is now the **detailed design/contract for the shipped guard system**, with its build plan) and ECEF-nadir & direction-markers (design + build plan).
- Feature 1.3 TLE propagator (`build-plan-feature-1.3.md`; decisions: TLE home in `core/tle.py`, CelesTrak-only fetch, sample-grid promotion to `core/sampling.py`, the `Epoch.seconds_since` diff helper) and Feature 1.4 real-time tracker (`build-plan-feature-1.4.md`; decisions: full feature on one branch, the `fetch_tle` realtime-TTL selector, Claude self-sourced + cross-checked the topocentric/Sun-Moon reference data).
- Tier B drag `BoxFaceCd` (`build-plan-tier-b-drag.md`, 7 chunks — offline convergence study + Orekit-free generator + cross-validation gate, then the shipped per-face convex-box runtime; the front-loaded benefit-study **STOP** resolved GO / ship-with-caveat) plus `tier-b-drag-interpolation-findings.md` (the pre-build linear-interpolation analysis + convergence-study template; its "deferred" framing retired). Both retired to `docs/history/` once Tier B shipped (the binding design stays `general-upgrades-1.md` "Tier B Drag").
- Live Dashboard Blitting (`build-plan-live-dashboard-blitting.md`, 5 chunks — the additive primitive seams, then the mutate-in-place rewrite at `blit=False` that shipped the zoom/pan-survival headline; **Checkpoint A resolved STOP**, so the blit-layer Chunks 3–4 were deliberately not built and the wrap-up ran the trimmed docs-only Chunk 5). Retired to `docs/history/` at wrap-up (the binding design stays `general-upgrades-1.md` "Live Dashboard Blitting", with its Outcome note).
- ECEF InPlaneTracking (`build-plan-ecef-attitudes.md`, 4 chunks — the front-loaded benefit study ran the ecef leg through the *shipped stack* via a `CustomAttitude` stand-in law before the runtime existed (Checkpoint A → GO), then the pure-Python field + the primary-slot provider swap; the planned GEO failure-mode exception test was replaced by the build-verified "near-GEO completes" pin, and the shipped mode agreed with the stand-in to 0.02 km / 5 d). Retired to `docs/history/` at wrap-up (the binding design stays `general-upgrades-1.md` "ECEF InPlaneTracking", with its Outcome note).
- Civil Time Zones & Progress Reporting (`build-plan-ux-improvements.md`, 4 chunks — Part A tz end-to-end, then the headless `core/progress.py` reporter, then the step-handler wiring whose review widened the `try/finally` over the whole setup-through-classification region; Checkpoint A = the maintainer's interactive-terminal eyeball of the fine TTY cadence, the one path no automated context can see). Retired to `docs/history/` at wrap-up (the binding design stays `general-upgrades-1.md` "Civil Time Zones & Progress Reporting", with its Outcome note).
- `build-plan-stage-1.md` — the groundwork plan (Chunks 1–8): *why* the scaffolding is shaped the way it is.
- `docs/build-plan-feature-1.3,4-notes.md` (still in `docs/`, not archived) — prerequisite/scoping notes shared between 1.3 and 1.4 (Notes 1–5 + "Also fold in"; e.g. the sky view relocated from 1.3 to 1.4).

## Environment & running

The conda env named `propygator` already exists and runs. `environment.yml` is on disk and is the single source of truth for the env (it installs propygator editable via `pip: -e .[dev]`).

- conda env named `propygator`, Python **3.11** (pinned), OpenJDK **17** (bundled by conda)
- `orekit_jpype` 13.1.x, `jpype1` 1.5.x, NumPy 2.x, pandas 3.x — all from `conda-forge`
- Dev toolchain (pytest, ruff, mypy, pre-commit, nbstripout) comes from the `[dev]` extra. Latest verified snapshot: `docs/verified_environments/2026-06.txt`.
- **Never** use the legacy JCC-based `orekit` package (pulls OpenJDK 8).

Activate before running anything Python: `conda activate propygator`. On this Windows machine, PowerShell needs a one-time `conda init powershell`; the conda interpreter is at `C:\Users\hzhan\miniconda3\envs\propygator\python.exe`. **Activation is mandatory, not cosmetic** — NumPy here links Intel MKL whose runtime DLLs only resolve in the activated env, so calling that interpreter path directly hard-crashes on the first NumPy linear-algebra call (`@`, `np.linalg.*`) with `Windows fatal exception 0xC06D007F` instead of a Python error. For non-interactive runs prefer `conda run -n propygator <cmd>` (note: `conda run` rejects newlines in `python -c`; use a script file).

Commands:
- Tests: `conda run -n propygator pytest`; single test: `pytest tests/core/test_time.py::test_name`. `testpaths=["tests"]` keeps a bare `pytest` from recursing into the vendored `./orekit-data/` tree.
- The stack-compatibility test (guards NumPy↔JPype↔Orekit, starts the JVM): `pytest tests/test_stack_compat.py`. Requires orekit-data resolvable (see below).
- Lint/format/typecheck/hooks: `pre-commit run --all-files` (ruff-check + ruff-format + mypy + nbstripout + file hygiene). Same hooks run in CI.
- Lint or type-check alone: `ruff check src tests scripts` / `mypy` (mypy scope is `src` + `scripts` only — tests are not type-gated; `py.typed` is a promise about `src/`).
- Create/update env: `conda env create -f environment.yml` / `conda env update -f environment.yml --prune` (if pip deps desync, recreate rather than update).

**faulthandler is disabled** (`addopts = ["-p", "no:faulthandler"]` in `pyproject.toml`). Once the JVM is up, HotSpot deliberately raises/handles access violations internally; faulthandler misreads the benign JVM-teardown signal as a fatal crash and prints a spurious access-violation traceback at shutdown (exit code stays 0). Do not re-enable it.

### orekit-data

~500 MB, **not** committed, treated as an external dependency. Resolution order (implemented in `_orekit_init._resolve_data_path`): `OREKIT_DATA_PATH` env var (authoritative — set-but-missing **raises**, never falls back) → `~/.propygator/orekit-data/` → `./orekit-data/` → raise `OrekitDataMissingError` (the message carries the URL + remedies; never a raw Java trace). A copy currently lives at the repo root `./orekit-data/`; the canonical install location is `~/.propygator/orekit-data/`. Fetch it with `python scripts/download_orekit_data.py` (also used by CI on a cache miss). Download source: https://gitlab.orekit.org/orekit/orekit-data (branch `main`, not `master`). The test session aborts cleanly via an autouse, no-JVM data gate in `tests/conftest.py` if data is unresolvable.

## Architecture invariants

These are enforced at module boundaries; violating them is a bug (architecture §4, §10).

- **Orekit types stay internal.** Public APIs accept/return only propygator types (`Epoch`, `Frame`, `State`, `Trajectory`, `TLE`, `Orientation`, …). JPype/Java objects appear only inside implementations. `to_orekit()` methods annotate the Orekit return type under `if TYPE_CHECKING:` (with a runtime `else:` branch) so no runtime Orekit import leaks into the public type surface — see `Epoch.to_orekit` / `State.to_orekit` for the pattern.
- **Lazy JVM, one JVM per process.** Importing `propygator` must NOT start the JVM. It starts on the first Orekit-touching call (via `_ensure_started()`), or via explicit `propygator.init(vmargs=...)`. JPype cannot re-init the JVM, so `init()` with conflicting `vmargs` raises `JVMAlreadyStartedError`. The "safe before init" surface (all `Epoch`/`Frame`/`State`/config construction + validation; everything except `to_orekit()` and the propagate/fetch/track verbs) is asserted by the pure-Python `tests/core/*` suite — keep accidental Orekit imports out of it (import `jpype`/`orekit_jpype`/`pyhelpers` lazily *inside* functions, never at module top).
- **Units:** SI everywhere internally (m, s, rad, kg); human-friendly (km, deg, days) only at the user-facing boundary.
- **Frames are explicit.** Every `State`/`Trajectory` carries a `Frame`; no defaults, no automatic conversion on `State`-returning paths (users call `.to_frame(...)`). Functions returning non-frame-carrying types (`GeodeticPosition`, `Pass`, scalars) may convert internally. v1 frame set: EME2000 (`J2000` is an enum *alias* of the same member), ITRF, TEME.
- **Time:** all internal time is `Epoch` (carries an explicit `TimeScale`; UTC user-facing, TT for dynamics; conversions explicit). Stored as a two-part `(_int_seconds, _frac_seconds)` count of TAI-seconds-since-J2000 (computed via the TT route for exactness; see `core/time.py` module docstring). Naive `datetime`s rejected at the boundary. **UT1 is deferred** (needs EOP data from a running JVM) — it raises `NotImplementedError` everywhere.
- **Paths:** `pathlib.Path` throughout — no string concat, no `os.path.join`.
- **Logging, never prints:** `logger = logging.getLogger(__name__)` per module; top-level `__init__.py` attaches a `NullHandler`. **One sanctioned exception (v0.5.0):** the long-running verbs' progress output — implemented once in `core/progress.py` (`_ProgressReporter`), stderr-only, TTY-gated (non-TTY coarsens to 25/50/75 milestones), opt-out via `progress=False`, never stdout, never the root logger; `logger.info` milestones still fire regardless (architecture §Logging).
- **Frozen dataclasses** for the data model, with shape/dtype/finiteness validation in `__post_init__` (`State`, `Epoch`, `KeplerianElements`, `Orientation`, `GroundStation`, …). Array-backed value types (`State`, `Orientation`) defensively copy inputs, set them read-only, and define value-based `__eq__`/`__hash__` (the dataclass-generated `__eq__` raises on ndarray fields). `Trajectory` is intentionally **not** frozen (`eq=False`, caches an `Ephemeris`) but its backing arrays are set read-only and equality is by identity.

## Module structure & dependency rule

Modern `src/` layout: `src/propygator/{core,propagation,tle,tracking,plotting,io}/` (see architecture §5, §7 for the full tree). Top-level `__init__.py` re-exports `init`/`clear_cache`, the types, the exceptions, and the full shipped verb + config surface (Features 1.1/1.3/1.4 + `BoxFaceCd`, `USTimeZone`, `ProgressCallback`) — see "Where things live" for the inventory; `look_angles_track` / `observer_snapshot` stay reachable via `propygator.core.observation` (not top-level). Still to land as later features ship: `find_passes` (1.5), `fit_tle` (1.2). Documented import alias is `pgr` (`import propygator as pgr`).

Dependencies flow inward: `tracking/` → `propagation/`/`tle/` → `core/`. `io/` depends only on `core/` (this is why `GroundStation`/`Pass`/`GeodeticPosition` live in `core/observation.py`, not in `io/` or `tracking/`). Exceptions live in `core/exceptions.py` (the innermost layer) so any subpackage can raise/catch them. **Nothing imports `plotting/`** — it's a leaf, so the core stays testable headless.

## Conventions for this repo

- **Build order:** groundwork → 1.1 → 1.3 → 1.4 (shipped `v0.1.0`–`v0.4.0`) → **general upgrades** (Tier B `BoxFaceCd`; Live Dashboard Blitting as the mutate-in-place layer, blit declined at Checkpoint A; ECEF InPlaneTracking; lumped planetary third body, its ERP companion blocked on upstream Orekit; civil time zones + progress reporting — shipped together as `v0.5.0`) → 1.5 passes → 1.2 TLE fitter. See Project state for detail.
- **Signatures are the binding contract.** Implement each verb to the **exact signature in its contract** — `features.md` (§1.1/§1.3/§1.4, and §1.5/§1.2 when started), or `general-upgrades-1.md` "Tier B Drag" for `BoxFaceCd` — don't change it. (The one sanctioned post-freeze edit so far: v0.5.0 added `progress: bool | ProgressCallback = True` to §1.1, contract-documented in general-upgrades-1 "Civil Time Zones & Progress Reporting".) The only `NotImplementedError` that is a genuine permanent deferral is UT1 (needs EOP data).
- **Notebooks** live in `notebooks/` (NOT inside the package), numbered (`01_intro.ipynb`); the `nbstripout` pre-commit hook strips outputs — keep it.
- **Plotting backends:** Plotly for interactive 3D, matplotlib for everything else (2D timeseries, polar sky charts). Each `plot_*` returns the native figure. A bundled low-res Natural Earth coastline in `data/` (NOT cartopy) draws map overlays.
- **Testing** (architecture §11): JVM + orekit-data started once per session via the opt-in `orekit` fixture in `tests/conftest.py` (requested only by tests that touch Orekit; the autouse fixture is a *no-JVM* data gate so the pure-Python `tests/core/*` suite can assert "safe before init"). Reference cases are ISS TLEs and Vallado vectors; Keplerian round-trips checked to machine precision; plot snapshot tests. The 3-part stack-compatibility test guards the NumPy↔JPype↔Orekit boundary.
  - **One-JVM-per-process ordering:** `jpype.isJVMStarted()` is a monotonic global flag, so the `tests/core` `test_no_jvm_started` guards only hold while no JVM test has run yet in the process. A `pytest_collection_modifyitems` hook in `conftest.py` schedules every `orekit`-fixture test *last*, so any path order (e.g. `pytest tests/test_conversions.py tests/core`) stays green. **New JVM-touching tests must acquire the JVM via the `orekit` fixture** — that's how the hook knows to order them after the pure-Python guards.
- **Versioning:** SemVer from 0.1.0, staying on 0.x; version single-sourced in `pyproject.toml`, read back via `importlib.metadata` (`core.states._propygator_version`, returns `"unknown"` from a non-installed source tree). CHANGELOG is hand-maintained Keep-a-Changelog (conventions in `docs/changelog-guidelines.md`).
- **Credentials** (Space-Track) come from env vars (`SPACETRACK_USERNAME`/`PASSWORD`) or a gitignored `.env` (`.env.example` is committed); never committed.

## Java↔Python boundary notes (orekit_jpype)

- Import `jpype`/`orekit_jpype`/`orekit_jpype.pyhelpers` **lazily inside functions**, never at module top — `pyhelpers` does `from java.io import File` at import time and cannot even be imported before `initVM()`.
- `Vector3D` etc. are Java objects: extract with getters into NumPy (`np.array([v.getX(), v.getY(), v.getZ()])`).
- Cannot subclass Java classes from Python; implement Java interfaces (custom force models, event detectors, the variable-Cd `DragSensitive`) via `@JImplements`/`@JOverride`. Python proxies do **not** inherit Java `default` methods — implement every default the Java caller invokes, and test inside the real Orekit call path. Custom event detectors use `FunctionalDetector` + a 1-method `ToDoubleFunction` proxy + native `StopOnEvent` (not a raw `EventDetector` proxy) to dodge that default-method trap — reuse `_make_radius_stop_detector`.
- IDE autocomplete on `org.orekit.*` is limited (runtime stubs); red underlines that run fine are normal. mypy treats the `org.orekit.*`/`java.*`/`jpype`/`pandas`/`requests` namespaces as untyped (`ignore_missing_imports` in `pyproject.toml`).
- Don't `conda update` blindly — the pinned trio (Python 3.11 / OpenJDK 17 / orekit_jpype 13.1.x) is a tested combination; upgrade Orekit by creating a fresh env.
