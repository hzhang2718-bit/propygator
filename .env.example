# Space-Track credentials — OPTIONAL.
#
# Only needed for the Space-Track TLE source (Feature 1.3+). CelesTrak, the
# default source, needs no authentication. Copy this file to `.env` (which is
# gitignored) and fill in your credentials; propygator loads it via python-dotenv.

SPACETRACK_USERNAME=
SPACETRACK_PASSWORD=
